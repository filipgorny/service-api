import { WsEndpointConfig } from './types';

/**
 * Creates a WebSocket endpoint configuration
 * @param config - Endpoint configuration
 * @returns WebSocket endpoint config
 */
export function wsEndpoint(config: WsEndpointConfig): WsEndpointConfig {
  return config;
}
